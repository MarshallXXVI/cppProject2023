Our commandline argument is supposed to only take three arguments,
hence:

./solverMain a.options a.constraints

The copy-versions will be generated by PrettyPrinter(), which is a
method of the Parser class.
